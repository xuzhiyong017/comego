// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import java.util.Collections;
import java.util.List;

import static com.squareup.wire.Message.Label.REPEATED;

public final class ReportLogListRes extends Message {

  public static final List<ReportLogItem> DEFAULT_ITEMS = Collections.emptyList();

  @ProtoField(tag = 1, label = REPEATED)
  public final List<ReportLogItem> items;

  private ReportLogListRes(Builder builder) {
    this.items = immutableCopyOf(builder.items);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ReportLogListRes)) return false;
    return equals(items, ((ReportLogListRes) other).items);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    return result != 0 ? result : (hashCode = items != null ? items.hashCode() : 1);
  }

  public static final class Builder extends Message.Builder<ReportLogListRes> {

    public List<ReportLogItem> items;

    public Builder() {
    }

    public Builder(ReportLogListRes message) {
      super(message);
      if (message == null) return;
      this.items = copyOf(message.items);
    }

    public Builder items(List<ReportLogItem> items) {
      this.items = checkForNulls(items);
      return this;
    }

    @Override
    public ReportLogListRes build() {
      return new ReportLogListRes(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
