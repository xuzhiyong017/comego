// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.INT64;
import static com.squareup.wire.Message.Datatype.STRING;
import static com.squareup.wire.Message.Datatype.UINT64;

/**
 * ========== 这是一条推送消息 ===========
 */
public final class UserPromptMsg extends Message {

  public static final Long DEFAULT_REVISION = 0L;
  public static final String DEFAULT_MESSAGE = "";
  public static final String DEFAULT_URL = "";
  public static final Long DEFAULT_LIVE = 3L;

  @ProtoField(tag = 1, type = UINT64)
  public final Long revision;

  @ProtoField(tag = 2, type = STRING)
  public final String message;

  @ProtoField(tag = 3, type = STRING)
  public final String url;

  /**
   * URL
   */
  @ProtoField(tag = 4, type = INT64)
  public final Long live;

  private UserPromptMsg(Builder builder) {
    this.revision = builder.revision;
    this.message = builder.message;
    this.url = builder.url;
    this.live = builder.live;
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof UserPromptMsg)) return false;
    UserPromptMsg o = (UserPromptMsg) other;
    return equals(revision, o.revision)
        && equals(message, o.message)
        && equals(url, o.url)
        && equals(live, o.live);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = revision != null ? revision.hashCode() : 0;
      result = result * 37 + (message != null ? message.hashCode() : 0);
      result = result * 37 + (url != null ? url.hashCode() : 0);
      result = result * 37 + (live != null ? live.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<UserPromptMsg> {

    public Long revision;
    public String message;
    public String url;
    public Long live;

    public Builder() {
    }

    public Builder(UserPromptMsg message) {
      super(message);
      if (message == null) return;
      this.revision = message.revision;
      this.message = message.message;
      this.url = message.url;
      this.live = message.live;
    }

    public Builder revision(Long revision) {
      this.revision = revision;
      return this;
    }

    public Builder message(String message) {
      this.message = message;
      return this;
    }

    public Builder url(String url) {
      this.url = url;
      return this;
    }

    /**
     * URL
     */
    public Builder live(Long live) {
      this.live = live;
      return this;
    }

    @Override
    public UserPromptMsg build() {
      return new UserPromptMsg(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
