// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.ENUM;
import static com.squareup.wire.Message.Datatype.UINT32;

public final class GroupMember extends Message {

  public static final GroupMemberRoler DEFAULT_ROLER = GroupMemberRoler.GroupMemberRoler_None;
  public static final GroupMemberState DEFAULT_STATE = GroupMemberState.GroupMemberStateNone;
  public static final GroupMemberForbid DEFAULT_FORBID = GroupMemberForbid.GroupMemberForbidNone;
  public static final Integer DEFAULT_LOVERANK = 0;

  @ProtoField(tag = 1)
  public final UserInfo user;

  @ProtoField(tag = 2, type = ENUM)
  public final GroupMemberRoler roler;

  @ProtoField(tag = 4, type = ENUM)
  public final GroupMemberState state;

  @ProtoField(tag = 5, type = ENUM)
  public final GroupMemberForbid forbid;

  @ProtoField(tag = 6, type = UINT32)
  public final Integer loveRank;

  private GroupMember(Builder builder) {
    this.user = builder.user;
    this.roler = builder.roler;
    this.state = builder.state;
    this.forbid = builder.forbid;
    this.loveRank = builder.loveRank;
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof GroupMember)) return false;
    GroupMember o = (GroupMember) other;
    return equals(user, o.user)
        && equals(roler, o.roler)
        && equals(state, o.state)
        && equals(forbid, o.forbid)
        && equals(loveRank, o.loveRank);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = user != null ? user.hashCode() : 0;
      result = result * 37 + (roler != null ? roler.hashCode() : 0);
      result = result * 37 + (state != null ? state.hashCode() : 0);
      result = result * 37 + (forbid != null ? forbid.hashCode() : 0);
      result = result * 37 + (loveRank != null ? loveRank.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<GroupMember> {

    public UserInfo user;
    public GroupMemberRoler roler;
    public GroupMemberState state;
    public GroupMemberForbid forbid;
    public Integer loveRank;

    public Builder() {
    }

    public Builder(GroupMember message) {
      super(message);
      if (message == null) return;
      this.user = message.user;
      this.roler = message.roler;
      this.state = message.state;
      this.forbid = message.forbid;
      this.loveRank = message.loveRank;
    }

    public Builder user(UserInfo user) {
      this.user = user;
      return this;
    }

    public Builder roler(GroupMemberRoler roler) {
      this.roler = roler;
      return this;
    }

    public Builder state(GroupMemberState state) {
      this.state = state;
      return this;
    }

    public Builder forbid(GroupMemberForbid forbid) {
      this.forbid = forbid;
      return this;
    }

    public Builder loveRank(Integer loveRank) {
      this.loveRank = loveRank;
      return this;
    }

    @Override
    public GroupMember build() {
      return new GroupMember(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
