// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.STRING;
import static com.squareup.wire.Message.Datatype.UINT32;
import static com.squareup.wire.Message.Label.REQUIRED;

public final class MessageVideo extends Message {

  public static final String DEFAULT_PREFIX = "";
  public static final String DEFAULT_HASHS = "";
  public static final Integer DEFAULT_LEN = 0;
  public static final Integer DEFAULT_IFRAME = 0;
  public static final String DEFAULT_SUMMARY = "";

  @ProtoField(tag = 1, type = STRING)
  public final String prefix;

  @ProtoField(tag = 2, type = STRING, label = REQUIRED)
  public final String hashs;

  @ProtoField(tag = 3, type = UINT32)
  public final Integer len;

  @ProtoField(tag = 4, type = UINT32)
  public final Integer iframe;

  @ProtoField(tag = 5, type = STRING)
  public final String summary;

  private MessageVideo(Builder builder) {
    this.prefix = builder.prefix;
    this.hashs = builder.hashs;
    this.len = builder.len;
    this.iframe = builder.iframe;
    this.summary = builder.summary;
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof MessageVideo)) return false;
    MessageVideo o = (MessageVideo) other;
    return equals(prefix, o.prefix)
        && equals(hashs, o.hashs)
        && equals(len, o.len)
        && equals(iframe, o.iframe)
        && equals(summary, o.summary);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = prefix != null ? prefix.hashCode() : 0;
      result = result * 37 + (hashs != null ? hashs.hashCode() : 0);
      result = result * 37 + (len != null ? len.hashCode() : 0);
      result = result * 37 + (iframe != null ? iframe.hashCode() : 0);
      result = result * 37 + (summary != null ? summary.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<MessageVideo> {

    public String prefix;
    public String hashs;
    public Integer len;
    public Integer iframe;
    public String summary;

    public Builder() {
    }

    public Builder(MessageVideo message) {
      super(message);
      if (message == null) return;
      this.prefix = message.prefix;
      this.hashs = message.hashs;
      this.len = message.len;
      this.iframe = message.iframe;
      this.summary = message.summary;
    }

    public Builder prefix(String prefix) {
      this.prefix = prefix;
      return this;
    }

    public Builder hashs(String hashs) {
      this.hashs = hashs;
      return this;
    }

    public Builder len(Integer len) {
      this.len = len;
      return this;
    }

    public Builder iframe(Integer iframe) {
      this.iframe = iframe;
      return this;
    }

    public Builder summary(String summary) {
      this.summary = summary;
      return this;
    }

    @Override
    public MessageVideo build() {
      checkRequiredFields();
      return new MessageVideo(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
