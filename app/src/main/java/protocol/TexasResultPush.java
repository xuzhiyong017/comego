// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import java.util.Collections;
import java.util.List;

import static com.squareup.wire.Message.Datatype.UINT32;
import static com.squareup.wire.Message.Datatype.UINT64;
import static com.squareup.wire.Message.Label.REPEATED;
import static com.squareup.wire.Message.Label.REQUIRED;

/**
 * 游戏结果推送
 */
public final class TexasResultPush extends Message {

  public static final Long DEFAULT_ROOMID = 0L;
  public static final Integer DEFAULT_GAMEID = 0;
  public static final List<SeatResult> DEFAULT_SEATS = Collections.emptyList();

  @ProtoField(tag = 1, type = UINT64, label = REQUIRED)
  public final Long roomId;

  @ProtoField(tag = 2, type = UINT32, label = REQUIRED)
  public final Integer gameId;

  @ProtoField(tag = 3, label = REPEATED)
  public final List<SeatResult> seats;

  private TexasResultPush(Builder builder) {
    this.roomId = builder.roomId;
    this.gameId = builder.gameId;
    this.seats = immutableCopyOf(builder.seats);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof TexasResultPush)) return false;
    TexasResultPush o = (TexasResultPush) other;
    return equals(roomId, o.roomId)
        && equals(gameId, o.gameId)
        && equals(seats, o.seats);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = roomId != null ? roomId.hashCode() : 0;
      result = result * 37 + (gameId != null ? gameId.hashCode() : 0);
      result = result * 37 + (seats != null ? seats.hashCode() : 1);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<TexasResultPush> {

    public Long roomId;
    public Integer gameId;
    public List<SeatResult> seats;

    public Builder() {
    }

    public Builder(TexasResultPush message) {
      super(message);
      if (message == null) return;
      this.roomId = message.roomId;
      this.gameId = message.gameId;
      this.seats = copyOf(message.seats);
    }

    public Builder roomId(Long roomId) {
      this.roomId = roomId;
      return this;
    }

    public Builder gameId(Integer gameId) {
      this.gameId = gameId;
      return this;
    }

    public Builder seats(List<SeatResult> seats) {
      this.seats = checkForNulls(seats);
      return this;
    }

    @Override
    public TexasResultPush build() {
      checkRequiredFields();
      return new TexasResultPush(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
