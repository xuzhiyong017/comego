// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import java.util.Collections;
import java.util.List;

import static com.squareup.wire.Message.Datatype.ENUM;
import static com.squareup.wire.Message.Datatype.UINT64;
import static com.squareup.wire.Message.Label.REPEATED;
import static com.squareup.wire.Message.Label.REQUIRED;

public final class UserRoot extends Message {

  public static final Long DEFAULT_UID = 0L;
  public static final List<RootType> DEFAULT_ROOTTYPE = Collections.emptyList();

  @ProtoField(tag = 1, type = UINT64, label = REQUIRED)
  public final Long uid;

  @ProtoField(tag = 2, type = ENUM, label = REPEATED)
  public final List<RootType> rootType;

  private UserRoot(Builder builder) {
    this.uid = builder.uid;
    this.rootType = immutableCopyOf(builder.rootType);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof UserRoot)) return false;
    UserRoot o = (UserRoot) other;
    return equals(uid, o.uid)
        && equals(rootType, o.rootType);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = uid != null ? uid.hashCode() : 0;
      result = result * 37 + (rootType != null ? rootType.hashCode() : 1);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<UserRoot> {

    public Long uid;
    public List<RootType> rootType;

    public Builder() {
    }

    public Builder(UserRoot message) {
      super(message);
      if (message == null) return;
      this.uid = message.uid;
      this.rootType = copyOf(message.rootType);
    }

    public Builder uid(Long uid) {
      this.uid = uid;
      return this;
    }

    public Builder rootType(List<RootType> rootType) {
      this.rootType = checkForNulls(rootType);
      return this;
    }

    @Override
    public UserRoot build() {
      checkRequiredFields();
      return new UserRoot(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
