// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import java.util.Collections;
import java.util.List;

import static com.squareup.wire.Message.Label.REPEATED;

public final class UserInfoListRes extends Message {

  public static final List<UserInfo> DEFAULT_USERS = Collections.emptyList();

  @ProtoField(tag = 1, label = REPEATED)
  public final List<UserInfo> users;

  private UserInfoListRes(Builder builder) {
    this.users = immutableCopyOf(builder.users);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof UserInfoListRes)) return false;
    return equals(users, ((UserInfoListRes) other).users);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    return result != 0 ? result : (hashCode = users != null ? users.hashCode() : 1);
  }

  public static final class Builder extends Message.Builder<UserInfoListRes> {

    public List<UserInfo> users;

    public Builder() {
    }

    public Builder(UserInfoListRes message) {
      super(message);
      if (message == null) return;
      this.users = copyOf(message.users);
    }

    public Builder users(List<UserInfo> users) {
      this.users = checkForNulls(users);
      return this;
    }

    @Override
    public UserInfoListRes build() {
      return new UserInfoListRes(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
