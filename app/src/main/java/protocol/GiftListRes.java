// Code generated by Wire protocol buffer compiler, do not edit.
// Any Question Please Contact: jerryzhou@outlook.com
package protocol;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import java.util.Collections;
import java.util.List;

import static com.squareup.wire.Message.Datatype.INT64;
import static com.squareup.wire.Message.Datatype.UINT32;
import static com.squareup.wire.Message.Label.REPEATED;
import static com.squareup.wire.Message.Label.REQUIRED;

public final class GiftListRes extends Message {

  public static final Integer DEFAULT_INDEX = 0;
  public static final Integer DEFAULT_TOTAL = 0;
  public static final List<GiftInfo> DEFAULT_GIFTS = Collections.emptyList();
  public static final Long DEFAULT_VERSION = 0L;

  @ProtoField(tag = 1, type = UINT32, label = REQUIRED)
  public final Integer index;

  @ProtoField(tag = 2, type = UINT32, label = REQUIRED)
  public final Integer total;

  @ProtoField(tag = 3, label = REPEATED)
  public final List<GiftInfo> gifts;

  @ProtoField(tag = 4, type = INT64)
  public final Long version;

  private GiftListRes(Builder builder) {
    this.index = builder.index;
    this.total = builder.total;
    this.gifts = immutableCopyOf(builder.gifts);
    this.version = builder.version;
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof GiftListRes)) return false;
    GiftListRes o = (GiftListRes) other;
    return equals(index, o.index)
        && equals(total, o.total)
        && equals(gifts, o.gifts)
        && equals(version, o.version);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = index != null ? index.hashCode() : 0;
      result = result * 37 + (total != null ? total.hashCode() : 0);
      result = result * 37 + (gifts != null ? gifts.hashCode() : 1);
      result = result * 37 + (version != null ? version.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<GiftListRes> {

    public Integer index;
    public Integer total;
    public List<GiftInfo> gifts;
    public Long version;

    public Builder() {
    }

    public Builder(GiftListRes message) {
      super(message);
      if (message == null) return;
      this.index = message.index;
      this.total = message.total;
      this.gifts = copyOf(message.gifts);
      this.version = message.version;
    }

    public Builder index(Integer index) {
      this.index = index;
      return this;
    }

    public Builder total(Integer total) {
      this.total = total;
      return this;
    }

    public Builder gifts(List<GiftInfo> gifts) {
      this.gifts = checkForNulls(gifts);
      return this;
    }

    public Builder version(Long version) {
      this.version = version;
      return this;
    }

    @Override
    public GiftListRes build() {
      checkRequiredFields();
      return new GiftListRes(this);
    }
  }

  public static Builder newBuilder() {
    return new Builder();
  }
}
